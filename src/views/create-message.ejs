<!DOCTYPE html>
<html>
  <head>
    <title>Create Message</title>
    <style>
      body {
        display: flex;
        justify-content: center;
        align-items: flex-start;
        min-height: 100vh;
        margin: 0;
        background-color: #f5f5f5;
        font-family: Arial, sans-serif;
        padding: 2rem;
      }
      .container {
        width: 100%;
        max-width: 600px;
        background: white;
        padding: 2rem;
        border-radius: 10px;
        box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.1);
      }
      h1 {
        text-align: center;
        color: #a74fff;
        margin-bottom: 1.5rem;
      }
      form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }
      input, textarea {
        padding: 10px;
        border-radius: 5px;
        border: 1px solid #ccc;
        font-size: 1rem;
        width: 100%;
        box-sizing: border-box;
      }
      button {
        padding: 12px;
        background: #a74fff;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 1rem;
      }
      button:hover {
        background: rebeccapurple;
      }
      .back-link {
        display: inline-block;
        margin-bottom: 1rem;
        text-decoration: none;
        color: #a74fff;
      }
      .back-link:hover {
        text-decoration: underline;
      }
      .error-list {
        background: #ffe6e6;
        border: 1px solid #ff4d4d;
        border-radius: 5px;
        padding: 10px;
        margin-bottom: 1rem;
      }
      .error-list li {
        color: #d8000c;
        font-size: 0.95rem;
        margin-bottom: 4px;
      }
      li {
        list-style: none;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <a class="back-link" href="/">&#8592; Back to Home</a>
      <h1>Create New Message</h1>

      <% if (errors && errors.length > 0) { %>
        <ul class="error-list">
          <% errors.forEach(error => { %>
            <li><%= error.msg %></li>
          <% }) %>
        </ul>
      <% } %>

      <form action="/messages" method="POST">
        <input type="text" name="title" placeholder="Message Title"  value="<%= oldInput.title ? oldInput.title : '' %>" />
        <textarea name="message" rows="4" placeholder="Write your message..." ><%= oldInput.message ? oldInput.message : '' %></textarea>
        <button type="submit">Post Message</button>
      </form>
    </div>
  </body>
</html>
